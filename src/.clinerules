# 環境設定
あなたの実行環境は Windows PowerShell です。
コマンドを提案・実行する際は、必ず PowerShell の構文に従ってください。

# 注意点
- コマンドプロンプトの `&&` は使用せず、代わりに `;` または `if ($?) { ... }` を使用してください。
- `tail` や `grep` などの Linux コマンドはそのまま使用せず、`Select-Object` や `Select-String` などの PowerShell の代替コマンド（またはエイリアスが正しく機能する構文）を使用してください。